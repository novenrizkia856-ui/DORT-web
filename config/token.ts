/* ===========================================================================
 *
 *   THE TOKEN ADDRESS. THIS LINE IS THE WHOLE LAUNCH SWITCH.
 *
 *   Paste the address between the quotes, save, commit, push. Nothing else
 *   anywhere needs touching. The bar at the top of the site stops saying
 *   "Coming soon" and starts showing the address, with the copy button and
 *   the explorer link both live.
 *
 *   Empty means not launched. That is the only meaning of empty.
 *
 *   The build refuses to start if what you paste is not a well formed
 *   address, so a typo shows up here in seconds rather than on the live site.
 *
 * ======================================================================== */

export const TOKEN_ADDRESS = "";
